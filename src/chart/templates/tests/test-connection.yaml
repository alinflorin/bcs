apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bcs.fullname" . }}-test-connection"
  labels:
    {{- include "bcs.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bcs.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
