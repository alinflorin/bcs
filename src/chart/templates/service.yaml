apiVersion: v1
kind: Service
metadata:
  name: {{ include "bcs.fullname" . }}
  labels:
    {{- include "bcs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bcs.selectorLabels" . | nindent 4 }}
