name: Manual Uninstall

on:
  workflow_dispatch:
    inputs:
      environment:
        description: "Select the environment to uninstall"
        required: true
        type: choice
        options:
          - stg
          - uat
          - prd

jobs:
  uninstall:
    runs-on: ubuntu-latest
    environment:
      name: ${{ github.event.inputs.environment }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Write Kubeconfig to file
        run: |
          echo "${{ secrets.KUBECONFIG }}" > kubeconfig.yaml

      - name: Setup Tailscale
        uses: tailscale/github-action@v3
        with:
          oauth-client-id: ${{ secrets.TAILSCALE_CLIENT_ID }}
          oauth-secret: ${{ secrets.TAILSCALE_CLIENT_SECRET }}
          use-cache: "true"
          tags: tag:mobile
          version: latest

      - name: Log in to GHCR for Helm
        run: |
          echo ${{ secrets.GITHUB_TOKEN }} | helm registry login ghcr.io --username ${{ github.actor }} --password-stdin

      - name: Uninstall Helm release from ${{ github.event.inputs.environment }}
        run: |
          helm uninstall \
            --kubeconfig ${{ github.workspace }}/kubeconfig.yaml \
            --namespace bcs-${{ github.event.inputs.environment }} \
            bcs || echo "Release not found, skipping"

      - name: Remove kubeconfig file
        if: always()
        run: rm -rf ${{ github.workspace }}/kubeconfig.yaml || true
