name: bcs-ui Docker CI

on:
  push:
    branches:
      - main
    paths: 
      - src/bcs-ui/**

  pull_request:
    branches:
      - main
    paths: 
      - src/bcs-ui/**

jobs:
  reusable-docker:
    permissions:
      actions: read
      contents: read
      security-events: write
      packages: write
    uses: ./.github/workflows/reusable-docker-ci.yaml
    with:
      project: 'bcs-ui'
      ci: true
      version: ${{ github.sha }}
    secrets: inherit